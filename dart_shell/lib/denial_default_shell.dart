/// Denial's stock shell feature composition.
///
/// Custom products should import `package:denial_dart_shell/denial.dart` and
/// assemble their own [DenialShell] instead.
library;

export 'src/features/default_shell/default_shell_app.dart';
