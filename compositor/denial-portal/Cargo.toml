[package]
name = "denial-portal"
version = "0.0.0"
edition = "2024"
license = "GPL-3.0-or-later"
publish = false

[dependencies]
denial-core = { package = "denial", path = ".." }
libc = "0.2"
tracing = "0.1.37"
tracing-subscriber = { version = "0.3.16", features = ["env-filter"] }
zbus = "=5.18.0"
